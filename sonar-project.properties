# Required metadata
sonar.projectKey=saurav
sonar.projectName=saurav
sonar.projectVersion=1.0

# Source directories
sonar.sources=src  # Adjust based on your project structure

# Language-specific configuration (adjust as per your codebase)
sonar.language=java  # Example for Java, change according to your language
sonar.java.source=1.8  # Adjust for Java version or remove for other languages

# Additional analysis settings (optional)
sonar.exclusions=**/*.test.js,**/*.spec.js  # Optional: exclude files if needed
