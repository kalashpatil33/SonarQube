# Required metadata
sonar.projectKey=saurav
sonar.projectName=saurav
sonar.projectVersion=1.0

# Source information
sonar.sources=.
sonar.sourceEncoding=UTF-8

# Connection timeout settings (updated property names)
sonar.scanner.socketTimeout=300
sonar.scanner.connectTimeout=300

# Network settings
sonar.scanner.keepAlive=true
sonar.scanner.maxRetries=3
sonar.scanner.retryDelay=1000

# Optional - Skip specific files/directories
#sonar.exclusions=**/node_modules/**,**/dist/**
